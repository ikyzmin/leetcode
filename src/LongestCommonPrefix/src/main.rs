use std::collections::HashMap;

fn main() {
    println!("Hello, world!");
}

pub fn longest_common_prefix(strs: Vec<String>) -> String {
    let mut result = String::new();
    let mut entry_hash_map = HashMap::new();
    let mut letter_index = 0;
    for word in strs {

    }
    result
}